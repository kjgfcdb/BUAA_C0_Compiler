const int N = 100, M = -10000;
const int K = 0;
const char ch1 = '+', ch2 = '-', ch3 = '*', ch4 = '/', ch5 = '_', ch6 = 'a', ch7 = '0', ch8 = 'c', ch9 = '9';
int a[100],  i, j, n, m, k, x, y, z, mx;
char ch, s[100];
int mymax(int x, int y){
    int ans;
    ans = x;
    if (y > x){
        ans = y;
    }
    else
    ;
    return (ans);
}
int factorial(int n){ 
    int res;
    if (n < 0)
        return (-1);
    switch (n){
        case 0: res = 1; 
        case 1: res = 1; 
        default:  res = (n*factorial(n-1));
    }
    return (res);
}
int C(int m, int n){ 
    int x, y, z;
    if (m <= n) ;
    
    if (m > n)
        return (-1);
    return (factorial(n)/factorial(m)/factorial(n-m));
}
void init(){
    printf("Please enter n,m,k,ch:");
    m = 5;
    scanf(n,m,k, ch);
    printf("\n");
    return ;
}
char work(int m, int n){
    int ans;
    printf("work!!!");
    ans = C(m,n);
    switch (ans - 2*(ans / 2)){ 
        case 1: return ('O'); 
        default: return ('E'); 
    }
    return ('*');
}

void test_switch(){
    int Int, a[1000];
    printf("\ntest_switch\n");
    a[2] = -1;
    a[1] = -2;
    Int = 2*5/4;
    printf("Int=", Int);
    Int = a[2*5/4];
    printf("Int=", Int);
    Int = a[2+a[2*5/4]];
    printf("Int=", Int);
    Int = a[a[2+a[2*5/4]]+3];
    printf("Int=", Int);
    switch (Int){
        case -1: printf("666666\n");
        case -2: {printf("HHHHH\n"); }
        default: ;
    }

    switch (Int+1+'b'){
        case 1: Int = Int+1;
        case 97: {printf("     23333");  }
        case -123: {printf("        *****",Int);  printf(Int); }
        default: {printf("     None");}
    }
}

void test_for(int Int){
    Int = 0;
    printf("Int=",Int);
    for (i = Int; i <= 1000-Int; i = i+1){
        for (i = Int; i <= i-1; i = i+1){
            Int = Int+1;
        }
    }
    printf("Int=",Int);
}

void main() {
    test_for(1);
    return;
}